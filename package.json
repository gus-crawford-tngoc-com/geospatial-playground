{
  "name": "interview-assessment",
  "version": "1.0.0",
  "description": "Describe app and fix a unit test",
  "main": "index.js",
  "scripts": {
    "prestart": "npm i",
    "start": "docker compose up -d && cd web && npm start",
    "test": "cd web && npm run coverage"
  },
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "@angular/cli": "^17.0.7",
    "@nestjs/cli": "^10.2.1"
  }
}
